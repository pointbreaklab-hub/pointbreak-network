id: messaging-inbox
name: Inbox
version: 0.1.0
enabled: true
description: Async encrypted message requests. Opt-in only, no cold outreach without acceptance.

routes:
  - path: /app/messages
    component: components/InboxList.svelte
    nav:
      label: Inbox
      icon: mail
      order: 50

permissions:
  - db:messages:read
  - db:messages:write

requires:
  core: '>=0.1.0'
