User-agent: *

# The whole /app subtree is behind the auth guard and renders client-side.
# There is nothing there for a crawler to read.
Disallow: /app/

# The landing page at / is the only public surface.
Allow: /$
